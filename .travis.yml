osx_image: xcode11.3
language: ruby
rvm: 2.7.0
jdk: oraclejdk8
before_install:
  - nvm install 10
install:
  - npm run ci.base.setup
  - npm run ci.pip.install
  - npm run ci.build.ios.setup
before_script: pod repo update
script:
  - fastlane ios build

cache: npm
